Générations certificats de sécurité :

sudo openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
-keyout selfsigned.key -out selfsigned.crt

Voir les ports utilisés :

sudo netstat -tuln
sudo lsof -i -P -n


	- FR
	- Occitanie
	- Perpignan
	- 42 Perpignan
	- Student
	- localhost / nom du domaine
	- email (momillio@student.42perpignan.fr)


	    # FRONTEND SPA
    location / {
      proxy_pass http://frontend:5173;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;

      try_files $uri /index.html;
    }

    # API: AUTH
    location /api/auth/ {
      proxy_pass http://auth:3001/;
      proxy_set_header Host $host;
    }

    # API: PROFILE
    location /api/profile/ {
      proxy_pass http://user-profile:3002/;
      proxy_set_header Host $host;
    }

    # API: MATCHMAKING
    location /api/matchmaking/ {
      proxy_pass http://matchmaking:3005/;
      proxy_set_header Host $host;
    }

    # WEBSOCKET: GAME
    location /ws/game/ {
      proxy_pass http://game:3003;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;
    }

    # WEBSOCKET: CHAT
    location /ws/chat/ {
      proxy_pass http://chat:3004;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;
    }


